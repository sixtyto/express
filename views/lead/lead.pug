extends ../layout

block content
  h1 Lead #{lead.id}
  p email: #{lead.email}
    | 
    a(href="/lead/"+lead.id+"/edit").btn.btn-secondary Edit
    |
    form(action="/lead/"+lead.id+"/delete" method="POST")
      button(type="submit").btn.btn-danger Delete